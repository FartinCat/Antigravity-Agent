interface:
  display_name: "Python Agent"
  short_description: "Specialist for Python code"
  brand_color: "#10B981"
  default_prompt: "Assist with Python development."
policy:
  allow_implicit_invocation: true


operations:
  arithmetic: ["add", "subtract", "multiply", "divide"]
  statistics: ["mean", "median", "stddev", "min", "max"]
  data_processing: ["filter", "map", "reduce", "sort", "find"]
  calculations: ["power", "sqrt", "log", "exp"]
