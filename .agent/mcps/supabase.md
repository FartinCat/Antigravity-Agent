# Supabase MCP Server

**Purpose:** Interface with Supabase projects (Postgres, Auth, Storage).
**Use Case:** Managing schemas, auth configurations, and database migrations directly from the agent runtime.
