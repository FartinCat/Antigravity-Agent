---
description: Audit and upgrade UI/UX to premium standards
---

Invoke `.agent/.agents/skills/13-web-aesthetics`.

Audit for:
- Layout consistency and visual hierarchy
- Color system (primary, accent, neutral — no rainbows)
- Typography (max 2 font sizes per view, bold for hierarchy)
- Spacing and whitespace as design decisions
- WCAG AA accessibility compliance
- Responsive design (mobile-first: 375px → 1440px)
- Loading states, focus states, hover states
- Motion and transitions (<200ms for interactions)

Implement:
- Clean, premium design that earns trust at first glance
- Smooth transitions that communicate state change
- Accessible colors with proper contrast ratios

Verify at **375px** (mobile) and **1440px** (desktop). Run accessibility audit.
